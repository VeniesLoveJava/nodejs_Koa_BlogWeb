const config = {
    // 启动端口
    port: 3000,
  
    // 数据库配置
    database: {
      DATABASE: 'mysql',
      USERNAME: 'root',
      PASSWORD: '1234',
      PORT: '3306',
      HOST: '127.0.0.1'
    }
  }
  
  module.exports = config